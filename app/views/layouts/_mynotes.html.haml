-@mynotes.each do |note|
  =link_to mynote_path(note),class: "note-tag" do
    .the-note
      %h3
        =note.title
      %p
        =truncate(note.body, length: 45)
      %p
        =time_ago_in_words(note.created_at)
        ago
