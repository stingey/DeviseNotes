%h2
  Notes for
  =@user.first_name

-@mynotes.each do |note|
  %h3
    =link_to note.title, mynote_path(note)
  %p
    =truncate(note.body, length: 15)
  %p
    =time_ago_in_words(note.created_at)
    ago
=link_to "all notes", notes_path
=link_to "new note", new_note_path
