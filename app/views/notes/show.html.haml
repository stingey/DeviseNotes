.container1
  %h2
    Note by
    =@user.first_name
    =render 'layouts/showstuff'


  -if user_signed_in?
    =render 'layouts/comment'
  -else
    =link_to "Sign in", new_user_session_path
    to comment!
